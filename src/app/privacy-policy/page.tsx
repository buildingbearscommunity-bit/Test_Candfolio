import SiteInfoPage from '@/components/SiteInfoPage';
import { siteInfoPages } from '@/lib/siteInfoPages';

export default function PrivacyPolicyPage() {
  return <SiteInfoPage page={siteInfoPages.privacyPolicy} />;
}
